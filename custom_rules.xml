<?xml version="1.0" encoding="UTF-8"?>
<project name="johttp-rules" default="help">

  <!-- A rule to generate the JAR for inclusion in an Android
       application. Output file will be bin/johttp.jar -->
  <target name="jar" depends="-compile">
    <jar destfile="bin/johttp.jar"
         basedir="bin/classes" />
  </target>
</project>
